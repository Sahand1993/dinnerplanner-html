var DishSearcherView = function(container, model){
    var container = container;
    var model = model;

    this.show = function(){
        container.show();
    }
    this.hide = function(){
        container.hide();
    }
}